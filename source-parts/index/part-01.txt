<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TONNAGE Staff</title>
  <style>
    :root{
      color-scheme: dark;
      --bg:#090b0e;
      --panel:#111419;
      --panel2:#15191e;
      --line:#30363e;
      --line2:#3b424b;
      --text:#f5f7f9;
      --muted:#8f969f;
      --dim:#5f6770;
      --accent:#ffffff;
      --danger:#d07070;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:Inter,Segoe UI,Arial,sans-serif;
      background:var(--bg);
      color:var(--text);
      min-height:100vh;
    }
    button,input,select{font:inherit}
    button{cursor:pointer}
    .app{display:grid;grid-template-columns:260px 1fr;min-height:100vh}
    .sidebar{
      border-right:1px solid var(--line);
      background:#0d1014;
      padding:24px 18px;
      position:sticky;top:0;height:100vh;
    }
    .brand-kicker{font-size:11px;letter-spacing:2px;color:var(--muted);font-weight:800}
    .brand{font-size:28px;font-weight:950;margin-top:6px}
    .brand-sub{font-size:12px;color:var(--muted);line-height:1.5;margin:8px 0 24px}
    .nav{display:flex;flex-direction:column;gap:8px}
    .nav button{
      text-align:left;border:1px solid transparent;background:transparent;color:var(--muted);
      padding:12px 13px;border-radius:12px;font-weight:800
    }
    .nav button.active{background:var(--panel2);border-color:var(--line);color:white}
    .sidebar-bottom{position:absolute;left:18px;right:18px;bottom:22px}
    .tiny{font-size:10px;color:var(--dim);line-height:1.5}
    .main{padding:28px;max-width:1300px;width:100%;margin:0 auto}
    .topbar{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:24px}
    .kicker{font-size:11px;letter-spacing:1.8px;color:var(--muted);font-weight:900}
    h1{font-size:34px;margin:6px 0 0}
    .actions{display:flex;flex-wrap:wrap;gap:10px;justify-content:flex-end}
    .btn{
      border:1px solid var(--line2);background:transparent;color:white;border-radius:12px;
      padding:11px 14px;font-weight:850
    }
    .btn.primary{background:white;color:#090b0e;border-color:white}
    .btn.danger{border-color:#6b4040;color:#e7a3a3}
    .card{
      background:var(--panel2);border:1px solid #1e242b;border-radius:16px;padding:18px;margin-bottom:14px
    }
    .grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px}
    .row{display:flex;align-items:center;gap:10px}
    .between{display:flex;align-items:center;justify-content:space-between;gap:12px}
    label{font-size:11px;color:var(--muted);font-weight:850;letter-spacing:.5px}
    input,select{
      width:100%;background:#0e1115;border:1px solid var(--line);color:white;border-radius:10px;
      padding:10px 11px;outline:none
    }
    input:focus,select:focus{border-color:#68717c}
    table{width:100%;border-collapse:collapse}
    th{font-size:10px;color:var(--muted);letter-spacing:.8px;text-align:left;padding:10px;border-bottom:1px solid var(--line)}
    td{padding:9px 10px;border-bottom:1px solid #232930;vertical-align:middle}
    tr:last-child td{border-bottom:0}
    .muted{color:var(--muted)}
    .empty{padding:38px 18px;text-align:center;color:var(--muted)}
    .pill{
      display:inline-flex;padding:5px 8px;border:1px solid var(--line);border-radius:999px;
      font-size:10px;color:var(--muted);font-weight:850
    }
    .plans-layout{display:grid;grid-template-columns:280px 1fr;gap:14px}
    .plan-list{display:flex;flex-direction:column;gap:8px}
    .plan-item{
      width:100%;text-align:left;border:1px solid var(--line);background:#101319;color:white;
      border-radius:12px;padding:12px
    }
    .plan-item.active{border-color:#69727c;background:#1a1f25}
    .plan-item-name{font-weight:900}
    .plan-item-meta{font-size:10px;color:var(--muted);margin-top:4px}
    .exercise-check{width:18px;height:18px}
    .sets-input{width:78px}
    .schedule-toolbar{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px}
    .date-main{font-weight:900}
    .date-sub{font-size:10px;color:var(--muted);margin-top:3px}
    .status{
      border:1px solid var(--line);background:#0e1115;padding:10px 12px;border-radius:12px;
      color:var(--muted);font-size:12px;margin-bottom:14px
    }
    .status.ok{border-color:#425246;color:#b9d0be}
    .status.bad{border-color:#6b4040;color:#e7a3a3}
    .section-title{font-size:13px;font-weight:950;letter-spacing:.7px;margin-bottom:12px}
    .split-note{font-size:11px;line-height:1.6;color:var(--muted)}
    .stats-toolbar{
      display:flex;align-items:end;justify-content:space-between;gap:12px;flex-wrap:wrap
    }
    .stats-controls{display:flex;gap:10px;align-items:end;flex-wrap:wrap}
    .stats-controls > div{min-width:160px}
    .metric-grid{
      display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin-bottom:14px
    }
    .metric{
      background:var(--panel2);border:1px solid #1e242b;border-radius:16px;padding:17px
    }
    .metric-label{font-size:9px;color:var(--muted);font-weight:950;letter-spacing:1px}
    .metric-value{font-size:25px;font-weight:950;margin-top:7px}
    .metric-sub{font-size:10px;color:var(--muted);margin-top:5px;line-height:1.45}
    .analytics-grid{display:grid;grid-template-columns:1.25fr .75fr;gap:14px}
    .chart{
      height:235px;display:flex;align-items:flex-end;gap:7px;padding-top:16px;
      border-top:1px solid #232a31;margin-top:12px;overflow:hidden
    }
    .bar-wrap{
      min-width:0;flex:1;height:100%;display:flex;flex-direction:column;justify-content:flex-end;
      align-items:stretch;gap:6px
    }
    .bar-value{font-size:8px;color:#78808a;text-align:center;white-space:nowrap}
    .bar{
      min-height:2px;background:#e7ebef;border-radius:5px 5px 2px 2px;opacity:.88;
      position:relative
    }
    .bar-label{
      font-size:8px;color:#68717a;text-align:center;white-space:nowrap;overflow:hidden;text-overflow:ellipsis
    }
    .hbar-list{display:flex;flex-direction:column;gap:11px;margin-top:12px}
    .hbar-row{display:grid;grid-template-columns:150px 1fr 82px;gap:10px;align-items:center}
    .hbar-name{font-size:10px;color:#d9dde1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
    .hbar-track{height:9px;background:#0b0e12;border:1px solid #252b32;border-radius:999px;overflow:hidden}
    .hbar-fill{height:100%;background:#dfe4e8;border-radius:999px;min-width:1px}
    .hbar-value{font-size:9px;color:var(--muted);text-align:right}
    .heatmap{display:grid;grid-template-columns:repeat(13,1fr);gap:5px;margin-top:14px}
    .heat-week{display:grid;grid-template-rows:repeat(7,1fr);gap:5px}
    .heat-day{
      aspect-ratio:1;background:#12161b;border:1px solid #232a31;border-radius:4px;
      min-width:8px
    }
    .heat-day.l1{background:#2f343a}.heat-day.l2{background:#555d65}
    .heat-day.l3{background:#8f969e}.heat-day.l4{background:#e4e8eb}
    .heat-legend{display:flex;justify-content:flex-end;gap:5px;align-items:center;margin-top:9px;font-size:9px;color:var(--muted)}
    .legend-cell{width:12px;height:12px;border-radius:3px;border:1px solid #232a31}
    .delta-up{color:#b8d1be}.delta-down{color:#e0aaaa}.delta-flat{color:var(--muted)}
    .stats-empty{
      padding:70px 20px;text-align:center;color:var(--muted);background:var(--panel2);
      border:1px solid #1e242b;border-radius:16px
    }
    .stats-empty strong{display:block;color:white;font-size:18px;margin-bottom:8px}
    .record-table td,.record-table th{white-space:nowrap}
    .exercise-detail-head{display:flex;align-items:end;justify-content:space-between;gap:12px;flex-wrap:wrap}
    .sparkline{width:100%;height:150px;margin-top:14px}
    .sparkline svg{width:100%;height:100%;overflow:visible}
    .sparkline-line{fill:none;stroke:#eceff2;stroke-width:2}
    .sparkline-dot{fill:#eceff2}
    .sparkline-grid{stroke:#242a31;stroke-width:1}
    .rank-list{display:flex;flex-direction:column;gap:8px;margin-top:10px}
    .rank-item{
      display:grid;grid-template-columns:32px 1fr auto;gap:10px;align-items:center;
      padding:9px 0;border-bottom:1px solid #232930
    }
    .rank-item:last-child{border-bottom:0}
    .rank-no{font-size:10px;color:#656d76;font-weight:950}
    .rank-name{font-size:11px;font-weight:850}
    .rank-meta{font-size:9px;color:var(--muted);margin-top:3px}
    .rank-value{font-size:11px;font-weight:900}
    .nowrap{white-space:nowrap}
    .autosave-box{
      margin-top:14px;padding:11px 12px;border:1px solid var(--line);border-radius:12px;
      background:#101319
    }
    .autosave-title{
      font-size:9px;letter-spacing:.9px;font-weight:950;color:#8f969f
    }
    .autosave-state{
      margin-top:4px;font-size:11px;font-weight:850;color:#dfe4e8
    }
    .autosave-note{
      margin-top:5px;font-size:9px;line-height:1.45;color:#626a73
    }
    .gear-btn{
      width:44px;height:44px;padding:0;font-size:19px;display:inline-flex;align-items:center;justify-content:center
    }
    .folder-manager{
      display:grid;grid-template-columns:1fr auto;gap:10px;align-items:end
    }
    .folder-list{
      display:flex;flex-wrap:wrap;gap:8px;margin-top:12px
    }
    .folder-chip{
      display:flex;align-items:center;gap:7px;border:1px solid var(--line);background:#101319;
      border-radius:999px;padding:6px 8px 6px 11px
    }
